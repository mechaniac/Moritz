/**
 * Sift — Moritz's extractable interface library.
 *
 * Generic UI primitives that can be donated to sigrid for reuse across
 * child apps (shapeAndPose, etc.). Each component renders the minimum
 * possible DOM — the rendered HTML should mirror the magdalena tree.
 */

export { IconGrid, type IconGridItem } from './IconGrid.js';
export { Section } from './Section.js';
export { Slider } from './Slider.js';
export { Check } from './Check.js';
